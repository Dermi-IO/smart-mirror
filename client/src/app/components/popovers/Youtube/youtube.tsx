import { Popover } from "../pop-over";

export const YoutubePopover: React.FC = () => {
   return ( 
    <Popover minWidth={600} minHeight={400}>
        <div>Youtube Content</div>
    </Popover>
   );
}