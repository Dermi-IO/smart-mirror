/** @type {import('../shared/types/mirror-config').MirrorConfig} */

const mirrorConfig = {
    name: 'Mirror Project',
    showGrid: false,
    tilesX: 5,
    tilesY: 5,
    rotateInterfaceDegrees: 0,
    mirrorPadding: 24,
}

export default mirrorConfig;